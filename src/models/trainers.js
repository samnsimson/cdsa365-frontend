import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const TrainersModel = createSlice({
  name: "trainers",
  initialState,
  reducers: {},
});

export default TrainersModel.reducer;
